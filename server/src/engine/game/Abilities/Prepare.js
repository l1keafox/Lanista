class Prepare {
    // These are for pre clash.
    // These are triggered by state of the battle peeps.
    // If (me/opp) (hp/morale/mp) is (<,>) (0-100%)
    // random?
    // if 1/(2,3,4,5,10) then do action
    // Every X rounds?
  
    constructor() {
      this.abilityName = "Prepare";
    }
    forClash(caster, target) {
      // This does need aff effects.
    }
  }