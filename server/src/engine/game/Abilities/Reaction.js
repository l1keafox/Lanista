
class Reaction {
    // These will react to the clash - WIN/LOSE/TIE will effect if these are triggered or not.
    // It can also ignore and do just stats.
    // This will take in doClash which will determine if the user is a winner or loser.
  
    constructor() {
      this.abilityName = "Reaction";
    }
    doClash(result) {
      // clashReactions are based on win/lose/tie condidtion of the clashes.
    }
  }
  